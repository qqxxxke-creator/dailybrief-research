# dependencies
/node_modules
/.worktrees/

# OS
.DS_Store

# env files
.env*
!.env.example

# trending pipeline output (each fork generates its own)
/daily_reports/
/logs/

# research intelligence runtime cache
/data/research/research-cache.json
/data/research/*.tmp
/data/research/*.corrupt-*

# Claude Code local state (skills + commands ARE committed; cache/state aren't)
/.claude/state/
/.claude/cache/

# typescript
*.tsbuildinfo

# screenshot originals (only the cropped versions are committed)
/docs/screenshots/originals/
